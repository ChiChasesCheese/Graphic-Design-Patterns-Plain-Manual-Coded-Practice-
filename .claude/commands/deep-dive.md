对 `src/$ARGUMENTS/` 目录下的所有 Java 源文件进行深度优化和知识点注释。

## 执行步骤

1. **读取所有文件**：读取该章节目录下全部 `.java` 文件（含子目录）和已有 `.md` 文件。

2. **优化**：以 2026 年实际工程最佳实践为标准，用 80/20 原则取舍——优先改动价值最高的地方，不过度设计，不为用特性而用特性。如果原代码已经是最佳实践，直接说明原因，不强行修改。

3. **注释原则**：只注释**为什么**这样写，不注释写了什么。只在最值得知道的地方加——反直觉的写法、常见陷阱、设计模式关键决策点。禁止注释显而易见的逻辑。

4. **输出格式**：
   - 先用 2-3 句话说清楚本章节模式的角色分工和核心机制
   - 逐文件说明：改了什么 → 为什么（只说关键点，不逐行解释）
   - 结尾列出 3-5 条**本章最值得记住的知识点**（80/20 精华）

## 硬性约束
- 最终代码必须在 IntelliJ（Java 23 + `--enable-preview`）中可运行
- 保持教科书模式的完整性，不引入当前章节范围之外的设计模式
